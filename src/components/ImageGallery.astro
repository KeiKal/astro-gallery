---
// Fetch images as before
import { fetchImages } from '../utils/api';

const images = await fetchImages();
---

<div class="container mx-auto px-4 py-6">
  <!-- Gallery Header -->
  <h1 class="text-2xl sm:text-3xl font-bold text-gray-800 text-center mb-8">
    Explore Stunning Photos
  </h1>

  {images.length === 0 ? (
    <p class="text-gray-500 text-center">No images found. Please try again later!</p>
  ) : (
    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-8">
      {images.map((image) => (
        <div 
          key={image.id} 
          class="group relative bg-white rounded-lg overflow-hidden shadow-md hover:shadow-xl transition-shadow transform hover:-translate-y-2"
        >
          <!-- Image Section -->
          <div class="relative overflow-hidden">
            <img
              src={image.urls.small}
              alt={image.alt_description || 'Image'}
              class="w-full h-52 object-cover transform transition-transform duration-500 group-hover:scale-110"
            />
            <!-- Gradient Overlay on Hover -->
            <div class="absolute inset-0 bg-gradient-to-b from-transparent to-gray-800 opacity-0 group-hover:opacity-80 transition-opacity duration-500"></div>
          </div>

          <!-- Card Content -->
          <div class="p-4 text-gray-800 relative z-10 bg-blue-500">
            <h2 class="text-lg font-semibold truncate text-gray-100 group-hover:text-white transition-colors duration-500">
              {image.alt_description || 'Beautiful Image'}
            </h2>
            <p class="text-sm text-gray-300 mt-2 group-hover:text-gray-200 transition-colors duration-500 line-clamp-2">
              {image.description || 'No description available.'}
            </p>
            <div class="mt-3 flex justify-between items-center">
              <a 
                href={image.urls.full} 
                target="_blank" 
                class="text-blue-400 hover:text-blue-500 text-sm font-medium group-hover:underline"
              >
                View Full Image
              </a>
              <span class="text-xs text-gray-400">
                By: {image.user.name || 'Unknown'}
              </span>
            </div>
          </div>
        </div>
      ))}
    </div>
  )}
</div>
